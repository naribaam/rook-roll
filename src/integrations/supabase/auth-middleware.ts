// Stub for Next.js project. The original TanStack Start middleware is not
// applicable here; auth is handled client-side via @/lib/auth and server-side
// via Supabase Edge Functions.
export const requireSupabaseAuth = null as unknown as never;
